# ================================================================================
# VARIABLES DE ENTORNO - ML_Analisis_Ecosistema_Dev
# ================================================================================
# Copia este archivo a `.env` y ajusta los valores según sea necesario:
#   cp .env.example .env
# ================================================================================

# Airflow UID para Docker (NO cambiar)
AIRFLOW_UID=50000

# Hosts y puertos de servicios locales
HOST=127.0.0.1
KEDRO_VIZ_PORT=4141
JUPYTER_PORT=8888

# Airflow Fernet Key (GENERAR TU PROPIA KEY)
# Ejecuta: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Luego descomenta y pega tu key:
# AIRFLOW_FERNET_KEY=GENERA_TU_PROPIA_KEY_AQUI

# Credenciales GCS (opcional, si no usas gcloud CLI)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Ejemplos de otras credenciales (NO usar valores reales aquí)
#DEV_S3_ACCESS_KEY=xxxx
#DEV_S3_SECRET_KEY=xxxx
#DB_USER=usuario
#DB_PASSWORD=contraseña






